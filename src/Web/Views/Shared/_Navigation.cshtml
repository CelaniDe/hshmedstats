@using hshmedstats.Application.Global
@using hshmedstats.Web
<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">

            <li class="@Html.IsSelected(controller: "Dashboard")">
                <a href="@Url.Action("Index", "Dashboard")"><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Dashboard</span></a>
            </li>

            @if (User.IsInRole(Roles.Admin))
            {
                <li class="@Html.IsSelected(controller: "User")">
                    <a href="@Url.Action("Index", "User")"><i class="fa fa-user"></i> <span class="nav-label" data-i18n="nav.metrics">User Management</span> </a>
                </li>
            }
        </ul>
    </div>
</nav>